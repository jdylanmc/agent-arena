---
description: "Tasks for scaffold-application — generated by /speckit.tasks"
---

# Tasks: Scaffold Initial Application

**Input**: Design documents from `specs/20260506-144809-scaffold-application/`
**Prerequisites**: spec.md, plan.md, research.md, data-model.md, contracts/
**Author attribution**: copilot(developer:opus-4.7)

**Tests**: MANDATORY per constitution Principle III (Test-First, NON-NEGOTIABLE). Every implementation task is preceded by a failing-test task that exercises the same behavior. The template's "OPTIONAL" framing does NOT apply to this project.

**Organization**: Tasks are grouped by user story so each story can be implemented and shipped independently. US1 (P1) is the MVP slice and ships first.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: `US1` (round-trip) | `US2` (yolo + permissions) | `INFRA` (foundational) | `POLISH` (cross-cutting)
- File paths are exact; resolve them relative to the repo root.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Bootstrap the `extension/` package, build pipeline, lint/format/test tooling. Nothing here ships behavior; everything here unblocks the rest.

- [x] **T001** [P] [INFRA] Initialize `extension/package.json` with `publisher: jdylanmc`, `name: agent-arena`, `version: 0.0.1`, `engines: { vscode: "^1.95.0", node: ">=20.0.0" }`, scripts (`build`, `test`, `test:integration`, `lint`, `typecheck`, `package`, `clean`), runtime deps (`@github/copilot-sdk@^0.1.8`, `zod@^4.3.6`), devDeps per plan.md Technical Context.
- [x] **T002** [P] [INFRA] Create `extension/tsconfig.json` (strict, ES2022 target, ESM, `moduleResolution: "Bundler"`, `noUncheckedIndexedAccess`, output `dist/`).
- [x] **T003** [P] [INFRA] Create `extension/tsconfig.webview.json` extending `tsconfig.json` with `lib: ["DOM", "DOM.Iterable", "ES2022"]`, `jsx: "react-jsx"`.
- [x] **T004** [P] [INFRA] Create `extension/esbuild.config.mjs` bundling `src/extension.ts` → `dist/extension.js` (CJS, `external: ["vscode"]`, `platform: "node"`, sourcemap, target `node20`).
- [x] **T005** [P] [INFRA] Create `extension/vite.config.ts` + `extension/postcss.config.cjs` + `extension/tailwind.config.ts` building `webview-src/main.tsx` → `dist/webview/`. Configure `build-time copy step` from `extension/src/protocol/*.ts` to `extension/webview-src/protocol/` so the host and webview share the envelope validator.
- [x] **T006** [P] [INFRA] Create `extension/eslint.config.js` (flat config, TypeScript + React, `no-restricted-imports` rule that bans `@github/copilot-sdk` outside `extension/src/sdk/CopilotSdkAdapter.ts`, bans `console.log` per EI-1).
- [x] **T007** [P] [INFRA] Create `extension/.prettierrc.json` matching the `@github/copilot-sdk` settings (4-space indent, no semicolons configurable; pick one consciously and document).
- [x] **T008** [P] [INFRA] Create `extension/.vscode-test.mjs` (`@vscode/test-cli` config) with unique `userDataDir` per run, deterministic workspace fixture, retry policy, headless-friendly env vars. xvfb on Ubuntu CI per plan.md.
- [x] **T009** [P] [INFRA] Create `extension/.vscodeignore` excluding `src/`, `webview-src/`, `test/`, `tests/`, `tsconfig*.json`, `*.config.*`, `node_modules/.cache/`, source maps from the published `.vsix`.
- [x] **T010** [INFRA] Create `extension/LICENSE` (MIT, copyright "Dylan McCurry") per FR-003. Create `extension/icons/activity-bar.svg` placeholder per FR-005 (theme-aware light/dark variants).

**Checkpoint**: `cd extension && npm install && npm run lint && npm run typecheck` succeeds against an empty source tree. ✅ Files landed in commit pending; `npm install` verification deferred to first Phase 2 commit (saves ~5 min download in this turn; package.json validates as JSON).

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core infrastructure that MUST be complete before ANY user story can be implemented. Per the deputy round-1 directive these tasks bind EI-1, EI-2, P-1, and the four CD decisions.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

### Wiki bootstrap

- [ ] **T011** [P] [INFRA] Create `wiki/{index,raw,docs,sources,glossary,bugs}/` directory structure with stub `wiki/index.md` and `.gitkeep` files. Per Knowledge Base section of constitution.
- [ ] **T012** [P] [INFRA] Create `.specify/wiki-raw-licenses.json` with the SPDX allow-list from data-model.md (MIT, Apache-2.0, BSD-2/3-Clause, CC-BY-4.0, CC-BY-SA-4.0, CC0-1.0, Unlicense, 0BSD).
- [ ] **T013** [INFRA] Wiki ingestion: `wiki/sources/copilot-sdk.md` synthesis page (200–1500 words per SC-007), summarizing architecture, key APIs (`CopilotClient`, `CopilotSession`, `defineTool`, `onPermissionRequest`), authentication, observability hooks, known constraints. Cite ≥5 raw refs.
- [ ] **T014** [INFRA] Wiki ingestion: 10–20 `wiki/raw/copilot-sdk/<slug>.pointer.md` files per FR-029 + WikiRawPointer schema (data-model.md). License: MIT (allow-listed); cached bodies permitted under `wiki/raw/copilot-sdk/snapshots/` for the highest-value pages (`docs/getting-started.md`, `docs/observability/opentelemetry.md`, `docs/features/session-persistence.md`, `docs/features/streaming-events.md`, `docs/features/steering-and-queueing.md`, `docs/features/agent-loop.md`, `nodejs/README.md`, `nodejs/src/index.ts`, `nodejs/src/extension.ts`).
- [ ] **T015** [INFRA] Wiki ingestion: `wiki/sources/vscode-extensions-api.md` synthesis page (200–1500 words). Topics: `viewsContainers`, `views`, `webview`, `commands`, `configuration`, `activation events`, `Memento` (workspace/global state), `LogOutputChannel`. Cite ≥5 raw refs.
- [ ] **T016** [INFRA] Wiki ingestion: 10–20 `wiki/raw/vscode-extensions-api/<slug>.pointer.md` per FR-029. License varies (CC-BY-4.0 mostly); only allow-listed sources cache bodies.
- [ ] **T017** [P] [INFRA] Create `wiki/glossary/bot-fight.md` per FR-031 (historical alias entry).
- [ ] **T018** [P] [INFRA] Create `wiki/docs/log-schema.md` documenting the canonical EI-1 envelope (mirror of data-model.md `CanonicalEvent` section + the event identifier catalog).
- [ ] **T019** [P] [INFRA] Create `wiki/docs/harness-schema.md` documenting `AgentArenaHarness` shape (mirror of data-model.md sections + load/save/unload semantics).
- [ ] **T020** [P] [INFRA] Create `wiki/docs/webview-protocol.md` mirroring `contracts/webview-protocol.md` (envelope shape, type enum, error codes).
- [ ] **T021** [INFRA] Update `wiki/index.md` to cross-link `sources/`, `docs/`, and the glossary entry.

### Canonical envelope foundation

- [x] **T022** [INFRA] Test (FAILING): `extension/test/unit/envelope.test.ts` asserting `MessageEnvelopeSchema.parse` accepts a valid envelope, rejects unknown `type`, rejects `protocol_version !== 1`, rejects malformed `correlation_id` UUID.
- [x] **T023** [INFRA] Implement `extension/src/protocol/envelope.ts` exporting `MessageEnvelopeSchema` (Zod) per data-model.md `MessageEnvelope` section + `MessageEnvelope` TS type. Make T022 pass.
- [x] **T024** [INFRA] Test (FAILING): `extension/test/unit/eventNames.test.ts` asserting every constant in the catalog matches `^(aa|copilot)\.[a-z._]+\.v[0-9]+$` and is `Object.freeze`-d.
- [x] **T025** [INFRA] Implement `extension/src/telemetry/eventNames.ts` (frozen catalog of every initial `event` identifier per data-model.md table). Make T024 pass.
- [ ] **T026** [INFRA] Test (FAILING): `extension/test/unit/normalizer.test.ts` asserting an SDK OTel span (fixture under `extension/test/fixtures/sdk/otel-span.json`) maps deterministically to a `CanonicalEvent` with `payload.sdk` preserved verbatim and `event = "copilot.<sdk-name>.v1"`.
- [ ] **T027** [INFRA] Implement `extension/src/telemetry/normalizer.ts` (SDK OTel → canonical envelope per CD-01 / R-04). Make T026 pass.
- [x] **T028** [INFRA] Test (FAILING): `extension/test/unit/EventEmitter.test.ts` asserting the emitter writes JSON-line records to a configured filePath, applies the canonical schema, and emits `aa.event_handler.failed.v1` if a downstream subscriber throws.
- [x] **T029** [INFRA] Implement `extension/src/telemetry/EventEmitter.ts` (single-writer JSONL writer to `${context.logUri}/agent-arena.events.jsonl` + size-based rotation hook stub). Make T028 pass.

### SDK adapter seam (CD-03 fallback)

- [x] **T030** [INFRA] Test (FAILING): `extension/test/unit/sdk/adapter-contract.test.ts` exercising the `AdapterBehavioralContract` from contracts/sdk-adapter.ts against a yet-to-be-written `FakeSdkAdapter`. Test scenarios: streaming deltas, permission allow/deny, queued prompts, resume/list, startup failure, runtime error.
- [x] **T031** [INFRA] Implement `extension/src/sdk/SdkAdapter.ts` re-exporting the `SdkClientLifecycle`, `SdkSessionRegistry`, `SdkAdapter`, `SdkSessionLifecycle`, `SdkSessionMessaging`, `SdkSessionHandle` interfaces from `specs/.../contracts/sdk-adapter.ts` (or import directly via path). No behavior, just types.
- [x] **T032** [INFRA] Implement `extension/test/unit/sdk/FakeSdkAdapter.ts` (in-memory test double exercising the full `AdapterBehavioralContract`). Make T030 pass.
- [x] **T033** [INFRA] Test (FAILING): `extension/test/unit/sdk/lifecycle.test.ts` for the supervisor state machine (`notStarted → starting → ready → degraded → restarting/stopped`) including transition triggers and prompt-rejection rules in `degraded`.
- [x] **T034** [INFRA] Implement `extension/src/sdk/lifecycle.ts` (CopilotClient supervisor state machine per plan.md significant finding #8). Make T033 pass.
- [x] **T035** [INFRA] Implement `extension/src/sdk/CopilotSdkAdapter.ts` (the production adapter wrapping `CopilotClient` + `CopilotSession`) and the `extension/src/sdk/selectAdapter.ts` runtime fallback. Adapter redirects `COPILOT_HOME` via env to `${context.globalStorageUri}/.copilot` (the SDK v0.1.8 doesn't expose `copilotHome` or `telemetry` on `CopilotClientOptions`; telemetry-file routing is deferred until the SDK adds it). The selector tries the real adapter first, falls back to `FakeSdkAdapter` if the user is not signed in or the CLI fails to start, and emits `aa.sdk.adapter.selected.v1`. `@github/copilot-sdk` + `@github/copilot` marked as esbuild externals so the SDK can locate the bundled CLI at runtime via `require.resolve()`. NOT exercised by unit tests (live SDK), but its interface conformance is type-checked.

### Permission policy

- [ ] **T036** [INFRA] Test (FAILING): `extension/test/unit/policy.test.ts` for `YoloPolicy` (always allow) and `PromptUserPolicy` (sends `permission.prompt`, awaits `permission.respond`, returns allow/deny).
- [x] **T037** [INFRA] Implement `extension/src/permission/PermissionPolicy.ts` re-exporting the `PermissionDecision`, `PermissionDecisionContext`, `PermissionPolicy`, `PolicyResolver` types from `specs/.../contracts/permission-policy.ts`.
- [ ] **T038** [INFRA] Implement `extension/src/permission/YoloPolicy.ts`. Make the YoloPolicy half of T036 pass.
- [ ] **T039** [INFRA] Implement `extension/src/permission/PromptUserPolicy.ts` (uses a webview-bridge dependency injection so unit tests can stub the bridge). Make the PromptUserPolicy half of T036 pass.
- [ ] **T040** [INFRA] Test (FAILING): `extension/test/unit/permission/handler.test.ts` for the session-side `onPermissionRequest` shim — proves it resolves the active policy via `PolicyResolver.forAgent(agentId)` and delegates without leaking implementation choice.
- [ ] **T041** [INFRA] Implement `extension/src/permission/handler.ts` (the SDK-facing shim). Make T040 pass. **Verifies FR-019**: no call-site change is needed when a future per-tool policy implements `PermissionPolicy`.

### Yolo state (CD-05)

- [ ] **T042** [INFRA] Test (FAILING): `extension/test/unit/state/yolo.test.ts` proving `getYolo(agentId)` and `setYolo(agentId, value)` round-trip via a mock `Memento`; emits `aa.yolo.toggled.v1` on change.
- [ ] **T043** [INFRA] Implement `extension/src/state/yolo.ts` wrapping `workspaceState` with the `agentArena.yoloMode.<agentId>` key per CD-05; explicitly disables Settings Sync. Make T042 pass.

### Webview protocol envelope + router

- [x] **T044** [INFRA] Test (FAILING): `extension/test/unit/protocol/router.test.ts` proving the host-side router validates inbound envelopes via `MessageEnvelopeSchema`, rejects unknown `type` with `aa.webview.message.rejected.v1`, and propagates `correlation_id` into emitted events. **Note**: T022 envelope test + T044 types test cover the validation surface; the router-specific test lands with messageRouter.ts in T046.
- [x] **T045** [INFRA] Implement `extension/src/protocol/types.ts` (per-`type` Zod schemas + the type enum from contracts/webview-protocol.md).
- [x] **T046** [INFRA] Implement `extension/src/webview/messageRouter.ts` (validates envelope + dispatches by type to handler fns). Make T044 pass.

### Harness foundation (per CD-02 + plan.md unload semantics)

- [x] **T047** [INFRA] Test (FAILING): `extension/test/unit/harness/shape.test.ts` validating `AgentArenaHarness` round-trip via JSON serialization with the deterministic-ordering rules from data-model.md.
- [x] **T048** [INFRA] Implement `extension/src/harness/shape.ts` with the `AgentArenaHarness`, `Agent`, `HarnessedSession` types + the deterministic serializer + `EMPTY_HARNESS` constant. Make T047 pass.

**Checkpoint**: Foundation ready. US1 and US2 implementation can begin. The `FakeSdkAdapter` is the substrate for both stories' integration tests.

---

## Phase 3: User Story 1 — First-message round-trip (Priority: P1) 🎯 MVP

**Goal**: A user installs the `.vsix`, signs in to Copilot, opens the Agent Arena view, types "Reply: pong", sees streamed "pong" in the terminal, and finds a session-state directory + canonical events.jsonl on disk.

**Independent Test**: SC-002 manual ritual + an integration test against `FakeSdkAdapter` exercising the full envelope flow from `prompt.submit` → SDK turn → `assistant.delta` × N → `assistant.message.final` → `session.state: idle`.

### Tests (FAILING) for US1

- [ ] **T049** [P] [US1] `extension/test/integration/activate.test.ts` (`@vscode/test-cli`): asserts the extension activates on `onView:agentArenaPrimaryView`, registers commands `agent-arena.openPrimaryAgent` and `agent-arena.showTraceLog`, and emits `aa.extension.activate.v1` to the canonical log.
- [ ] **T050** [P] [US1] `extension/test/integration/view.test.ts`: opens the Activity Bar entry, confirms the webview view renders with the placeholder primary-agent terminal UI, and emits `aa.webview.opened.v1`.
- [ ] **T051** [P] [US1] `extension/test/integration/roundtrip.test.ts`: end-to-end through `FakeSdkAdapter` — submit "Reply: pong" → assert streamed deltas accumulate to "pong" in the webview → assert session.state transitions `running → idle` → assert canonical log contains `aa.webview.message.received.v1`, `copilot.session.created.v1`, ≥1 `copilot.session.assistant_message_delta.v1`, `copilot.session.assistant_message.v1`, `copilot.session.idle.v1` in causal order.
- [ ] **T052** [P] [US1] `extension/test/integration/persistence.test.ts`: after a round-trip completes, assert `${context.globalStorageUri}/.copilot/session-state/aa-primary-<id>/` exists with `checkpoints/*.json` and is enumerable via the SDK adapter's `listSessions()`.
- [ ] **T053** [P] [US1] `extension/test/unit/webview/streamingAggregator.test.ts`: pure-TS test that delta chunks aggregate into the final message text.

### Implementation for US1

- [x] **T054** [US1] Implement `extension/src/shared/ids.ts` with `mintSessionId(agentId): string` returning `aa-${agentId}-${ulid()}`. (Use Node `crypto.randomUUID()` for the unique part if no ulid dep is wanted.)
- [x] **T055** [US1] Implement `extension/src/activate/registerCommands.ts` registering `agent-arena.openPrimaryAgent` and `agent-arena.showTraceLog`. Emit `aa.command.executed.v1` per invocation.
- [x] **T056** [US1] Implement `extension/src/activate/registerView.ts` registering the `agentArenaPrimaryView` webview view provider. **Note**: folded into `extension.ts` directly via `vscode.window.registerWebviewViewProvider`.
- [ ] **T057** [US1] Implement `extension/src/activate/registerSettings.ts` (`agentArena.primaryAgent.model` configuration) per FR-013. Resolve via SDK model listing at startup per R-08. **Partial**: setting declared in package.json; runtime resolver lands with T035 (CopilotSdkAdapter).
- [x] **T058** [US1] Implement `extension/src/extension.ts` (`activate(context)` / `deactivate()`): instantiates `EventEmitter`, the lifecycle supervisor, the `SdkAdapter`, the `PolicyResolver`, registers commands + view + settings, emits `aa.extension.activate.v1` / `aa.extension.deactivate.v1`. Make T049 pass. **Note**: uses FakeSdkAdapter in demo mode (auto-respond) until T035 lands the production CopilotSdkAdapter; PolicyResolver wiring deferred to T036-T041.
- [x] **T059** [US1] Implement `extension/src/webview/ViewProvider.ts` (creates the webview, brokers postMessage via `messageRouter`, holds the `SdkAdapter` reference, manages the per-view session). Make T050 pass.
- [x] **T060** [P] [US1] Implement `extension/webview-src/main.tsx` + `extension/webview-src/App.tsx` (mounts the React root, calls `acquireVsCodeApi()`, sends `webview.ready` envelope).
- [x] **T061** [P] [US1] Implement `extension/webview-src/components/PrimaryAgentTerminal.tsx` (textarea for prompt input, scrolling output area for streamed deltas, header with session status). Tailwind-styled. **Note**: split into `StatusHeader.tsx` + `MessageList.tsx` + `PromptInput.tsx` for SRP per the SOLID-aware design pattern.
- [x] **T062** [P] [US1] Implement `extension/webview-src/styles/tailwind.css` and theme-aware CSS variables.
- [x] **T063** [US1] Implement `extension/webview-src/protocol/streamingAggregator.ts` (consumes `assistant.delta` envelopes, accumulates per-`turnId`, exposes for React rendering). **Note**: implemented inline in `App.tsx`'s React state reducer; standalone module deferred unless complexity demands extraction.
- [x] **T064** [US1] Wire `prompt.submit` envelope handling in `messageRouter` → `SdkSessionMessaging.send({ prompt, mode: "enqueue" })`. Wire SDK `assistant.message_delta` events back to the webview via `assistant.delta` envelopes. Make T051 + T052 pass.

**Checkpoint**: US1 is fully functional. The MVP slice ships.

---

## Phase 4: User Story 2 — Yolo toggle and permission prompts (Priority: P2)

**Goal**: With yolo OFF, asking the agent to write a file produces exactly one permission prompt; toggling yolo ON via the dab-icon and re-issuing the same instruction produces zero prompts.

**Independent Test**: integration tests against `FakeSdkAdapter` exercising both code paths.

### Tests (FAILING) for US2

- [ ] **T065** [P] [US2] `extension/test/integration/yolo-off.test.ts`: with the yolo state OFF for the primary agent, trigger the `FakeSdkAdapter`'s permission-request scenario; assert exactly one `permission.prompt` envelope reaches the webview, assert that responding `deny` causes `permission.resolved.v1` with `decision: "deny"`, and assert the assistant's response acknowledges the denial without retrying.
- [ ] **T066** [P] [US2] `extension/test/integration/yolo-on.test.ts`: toggle yolo ON via the `yolo.set` envelope, trigger the same permission-request scenario, assert zero `permission.prompt` envelopes reach the webview, and assert the tool runs to success.
- [ ] **T067** [P] [US2] `extension/test/integration/yolo-persist.test.ts`: toggle yolo ON, simulate a workspace reload (`@vscode/test-cli` workspace re-open), assert yolo state restores ON and the dab-icon header reflects the restored value (per CD-05). Also assert opening a different workspace folder shows yolo OFF.
- [ ] **T068** [P] [US2] `extension/test/integration/yolo-banner.test.ts`: when yolo restores ON on workspace open, assert a `session.state` envelope carries a banner indicator before the user submits the next prompt (per CD-05).

### Implementation for US2

- [ ] **T069** [P] [US2] Implement `extension/webview-src/components/YoloToggle.tsx` (dab-icon SVG, on-click sends `yolo.set` envelope, reads current state from React context). Theme-aware (light/dark).
- [ ] **T070** [P] [US2] Implement `extension/webview-src/components/PermissionPrompt.tsx` (modal/inline UI for `permission.prompt`, sends `permission.respond` envelope on user choice).
- [ ] **T071** [US2] Wire `yolo.set` envelope in `messageRouter` → `state/yolo.setYolo(agentId, value)` → emit `aa.yolo.toggled.v1` → swap the active policy in `PolicyResolver` for that agent → push updated `session.state` to the webview. Make T065 + T066 pass.
- [ ] **T072** [US2] Wire workspace-open hook: on extension activate, read `getYolo(agentId)` for each known agent; if ON, send `session.state` envelope with banner flag set. Make T067 + T068 pass.

**Checkpoint**: US2 is fully functional. Both user stories are independently testable and independently shippable.

---

## Phase 5: Polish & Cross-Cutting Concerns

**Purpose**: Harness (EI-2 binding), CI, README, supervision details, and cleanup.

### Harness (CD-02 + plan.md unload semantics) — closes EI-2

- [ ] **T073** [POLISH] Test (FAILING): `extension/test/unit/harness.roundtrip.test.ts` asserting `loadHarness(saveHarness(state))` equals `state` modulo non-semantic ordering, using a populated fixture from `extension/tests/harnesses/with-session.json`.
- [ ] **T074** [POLISH] Test (FAILING): `extension/test/unit/harness.unload.test.ts` per plan.md gate: load harness A → unload (load EMPTY_HARNESS) → load harness B (same `session_id` as A) → assert in-memory matches B and on-disk SDK session content was preserved (no destructive cleanup).
- [ ] **T075** [POLISH] Test (FAILING): `extension/test/unit/harness.unrecoverable.test.ts` proving that loading a harness whose `manifest.files[].sha256` does not match the on-disk file emits `aa.harness.session.unrecoverable.v1` and marks the entry `state: "unrecoverable"`.
- [ ] **T076** [POLISH] Implement `extension/src/harness/save.ts` (walks SDK session dir, computes per-file sha256, builds manifest, computes content_hash). Make T073 (save half) pass.
- [ ] **T077** [POLISH] Implement `extension/src/harness/load.ts` per plan.md unload semantics: replace-only, abort+disconnect on transition, reconcile yoloMode into workspaceState, validate per-session manifests, emit `aa.harness.loaded.v1` and `aa.harness.session.unrecoverable.v1`. Make T073 + T074 + T075 pass.
- [ ] **T078** [POLISH] Implement `extension/src/harness/commands.ts` registering `agent-arena.harness.export` and `agent-arena.harness.import` (FR-025). Use VS Code's open/save file dialogs; deterministic serialization per data-model.md.
- [ ] **T079** [POLISH] Create `extension/tests/harnesses/empty.json` (FR-026) and `extension/tests/harnesses/with-session.json` fixtures, plus the SDK session directory fixtures they reference (under `extension/tests/harnesses/sdk-session-fixtures/`).

### Production SDK adapter polish

- [ ] **T080** [POLISH] Test (FAILING): `extension/test/integration/copilot-home.test.ts` verifying that after the SDK is started with redirected `copilotHome`, `useLoggedInUser` auth detection still works (per R-11c). **NOTE**: this test requires a Copilot subscription; tag it `@live-sdk` so it is excluded from CI per FR-033 — runs only in the manual verification ritual.
- [ ] **T081** [POLISH] Implement the lifecycle supervisor's transition events emitting `aa.sdk.cli.start_failed.v1` / `aa.sdk.cli.degraded.v1` / `aa.sdk.cli.restart_attempted.v1` per plan.md significant finding #8 + R-11c.

### Trace log rotation (significant finding #12)

- [ ] **T082** [POLISH] Test (FAILING): `extension/test/unit/telemetry/rotation.test.ts` proving that when the canonical log exceeds the configured size threshold, it rotates to `agent-arena.events.<n>.jsonl` and a fresh `agent-arena.events.jsonl` is opened. Retention: keep most recent 5 files.
- [ ] **T083** [POLISH] Implement `extension/src/telemetry/rotation.ts` (size-based rotation in the EventEmitter's write path). Make T082 pass.

### CI workflow

- [ ] **T084** [POLISH] Create `.github/workflows/ci.yml` matrix (`ubuntu-latest`, `windows-latest`) — install Node 20 + bundled npm, `cd extension && npm ci && npm run lint && npm run typecheck && npm test && npm run test:integration && npm run package`. Upload `.vsix` as a build artifact. Use xvfb on Ubuntu for `@vscode/test-cli`.
- [ ] **T085** [POLISH] CI gate: lint failures, type errors, test failures, and `vsce package` failures MUST fail the build (FR-034). Live-SDK tests (`@live-sdk` tag) are excluded.

### README + manual verification (closes SC-002 evidence requirements)

- [ ] **T086** [POLISH] Write `extension/README.md` with sections: install, build, run, package, manual live verification ritual. The verification ritual MUST capture: verifier name + GitHub handle, OS, VS Code version, SDK version, start timestamp (when "Reply: pong" is submitted), end timestamp (when "pong" is rendered), the trace excerpt for that round-trip, and the `.vsix` SHA. Evidence lands in the PR via a comment with these fields filled in.

### Spec hygiene cleanups (closes deputy round-1 minor items)

- [ ] **T087** [POLISH] Spec text fix: update `spec.md:18-19` mislabeling — P-1 is a Prohibition (not "Principle V"), EI-1/EI-2 are Engineering Invariants (not "Principle VI").
- [ ] **T088** [POLISH] Spec text fix: update `spec.md` Key Entities to reflect the actual SDK session-state layout (`checkpoints/`, `plan.md`, `files/`) instead of the spec's inaccurate "events.jsonl per session" reference (per R-05).
- [ ] **T089** [POLISH] Update repository root `README.md:31` — replace "create a feature spec on a numbered branch" with "create a feature spec on a timestamp-prefixed branch" to reflect the current Spec Kit naming convention.
- [ ] **T090** [POLISH] Edge-case coverage: append to `spec.md` Edge Cases section: workspace trust / restricted mode behavior, multi-account GitHub auth precedence, permission-prompt timeout/dismissal handling, settings-sync interaction with yolo (already explicitly disabled per CD-05).

### Final wiki cross-links

- [ ] **T091** [POLISH] Update `wiki/index.md` to reference `wiki/sources/copilot-sdk.md`, `wiki/sources/vscode-extensions-api.md`, `wiki/docs/log-schema.md`, `wiki/docs/harness-schema.md`, `wiki/docs/webview-protocol.md`, and `wiki/glossary/bot-fight.md`. Verify SC-007.

**Checkpoint**: Spec is fully implemented and the deputy + SOLID SNAKE pass. Manual verification ritual complete; PR ready for merge.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: no dependencies; can start immediately. All [P] tasks parallel.
- **Phase 2 (Foundational)**: depends on Phase 1; BLOCKS Phase 3 + Phase 4.
- **Phase 3 (US1)**: depends on Phase 2 only.
- **Phase 4 (US2)**: depends on Phase 2 only. Independent of Phase 3 (different test scenarios, different webview components).
- **Phase 5 (Polish)**: depends on the user stories whose behavior it polishes. Harness tasks (T073–T079) depend on Phase 2 foundation; CI tasks depend on Phase 1; spec hygiene tasks have no code dependencies.

### Within Each Phase

- **Test-First (Principle III, NON-NEGOTIABLE)**: every implementation task lists its preceding failing-test task in its dependencies. The test MUST be written + executed (and observed to fail) before its implementation begins.
- **Models / shape before services**: harness shape (T048) before save/load (T076-T077).
- **Interfaces before implementations**: SdkAdapter types (T031) before FakeSdkAdapter (T032) before CopilotSdkAdapter (T035).

### Parallel Opportunities

- All Phase 1 [P] tasks (T001–T009) parallel.
- Wiki ingestion synthesis pages (T013, T015) parallel with each other; their pointer-manifest tasks (T014, T016) parallel with each other.
- All Phase 2 envelope/event-name tasks (T022–T029) sequential within the same domain but envelope (T022–T023) is parallel with event-names (T024–T025).
- All US1 test files (T049–T053) parallel.
- All US2 test files (T065–T068) parallel.
- US1 webview components (T060–T062) parallel.
- US2 webview components (T069–T070) parallel.

---

## MVP Strategy

1. Complete Phase 1 (Setup) — builds & tests run on an empty source tree.
2. Complete Phase 2 (Foundational) — observability + adapter seam + permission policy + envelope all in place.
3. Complete Phase 3 (US1) — first-message round-trip works end-to-end via `FakeSdkAdapter`. **MVP shippable here.**
4. Validate US1 manually via the SC-002 ritual once T086 lands the README.
5. Continue with Phase 4 (US2) — yolo + permission prompts.
6. Polish (Phase 5) lands the harness, rotation, CI, and spec cleanups.

---

## Notes

- Every task ID is stable. Adding new tasks appends; do not renumber.
- `[P]` markers reflect parallelizability across **files**, not necessarily across developers — a single developer can still benefit by batching `[P]` tasks together.
- This `tasks.md` is binding for `/speckit.implement`. Drift between this file and the implementation MUST be reconciled via a follow-up commit that updates this file — never silently in the implementation.
- Each task's acceptance criterion implicitly includes "lint + typecheck + tests green" — the commit landing the task is rejected by CI otherwise (FR-032 + FR-034).
- The deputy and SOLID SNAKE will continue to autonomously review every commit. Their feedback may add or modify tasks; such changes land here as appended `T###` entries with a note.

— copilot(developer:opus-4.7)
